# موقع نور الهدى الإسلامي الشامل 🕌

موقع ويب إسلامي متكامل يحتوي على القرآن، الأحاديث، مواقيت الصلاة، حاسبة الزكاة، الأذكار، والمقالات. مبني باستخدام `Node.js / Express` للواجهة الخلفية و HTML/CSS/JS (Vanilla) للواجهة الأمامية بتصميم "Glassmorphism" متجاوب.

## المميزات الأساسية 🌟
- **القرآن الكريم:** تصفح لجميع السور والآيات.
- **الأحاديث النبوية:** مكتبة مقسمة لتصنيفات الأحاديث الصحيحة من كتب السُنة.
- **مواقيت الصلاة الدقيقة:** جلب أوتوماتيكي عبر Geolocation API و Aladhan API.
- **الأذكار والمسبحة:** قسم مخصص لأذكار الصباح والمساء مزود بمسبحة تنازلية.
- **حاسبة الزكاة:** أداة سريعة لحساب نصاب زكاة المال وفق ربع العشر.
- **المقالات الإسلامية:** تدوينات فقهية مفيدة.
- **دارك مود (Dark Mode):** متوفر بضغطة زر.

## الهيكلية التقنية 🛠️
- `frontend/`: الواجهة الأمامية المستقلة.
- `backend/`: واجهة الـ API (Express.js).
- `database/`: كود هيكلة قاعدة البيانات (MySQL).

## كيفية التشغيل محلياً 💻
1. قم بإنشاء قاعدة بيانات MySQL ونفذ سكريبت `database/database.sql` بداخلها لإنشاء الجداول والبيانات الوهمية.
2. توجّه لمجلد `backend`، قم بفتح التيرمينال (Terminal) واكتب: 
   `npm install`
3. ضف إعدادات قاعدة البيانات الخاصة بك في ملف `backend/.env` بناءً على `.env.example`.
4. ابدأ تشغيل السيرفر عن طريق:
   `node server.js`
5. السيرفر سيعمل على `http://localhost:5000`. ثم يمكنك فتح `frontend/index.html` في متصفحك أو عبر إضافة (Live Server).

## النشر عبر GitHub Pages و Vercel 🚀
- **للواجهة الأمامية (GitHub Pages):**
  المشروع يحتوي على `GitHub Actions` مبرمج مسبقاً في مجلد `.github/workflows/deploy.yml`، بمجرد رفع مشروعك الى Git (وبالنطاق الرئيسي `main`) سيتم نشر مجلد الـ frontend تلقائياً لصفحات جيتهاب.
  - للرفع: `git add .` -> `git commit -m "Initial Commit"` -> `git push origin main`.
  
- **للمشروع كاملاً (Vercel):**
  المشروع مزود بملف `vercel.json` جاهز. 
  يمكنك الدخول لـ Vercel.com وربط مستودع الـ GitHub به.. وسيقوم Vercel باستضافة الواجهة الأمامية و (Node.js Serverless) واجهة الـ Backend معاً بكل كفاءة وسرعة. 

---
**تم التطوير بعناية 💚 بواسطة Alking
**
