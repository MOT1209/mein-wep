# 🏋️ المدرب الذكي Pro - Full Stack Application

تطبيق ويب متكامل لللياقة والتغذية مع تقنيات الذكاء الاصطناعي

## 🏗️ هيكل المشروع

```
GymPro/
├── index.html          # واجهة المستخدم (Frontend)
├── styles.css         # الأنماط
├── app.js             # منطق التطبيق (Frontend)
├── server/            # الخادم (Backend)
│   ├── server.js      # خادم Express
│   ├── package.json   # تبعيات Node.js
│   ├── database.sql   # قاعدة البيانات
│   ├── Procfile       # للنشر
│   └── .env.example   # متغيرات البيئة
└── server/
```

---

## 🛠️ التقنيات المستخدمة

### Frontend
- **HTML5/CSS3** - واجهة مستخدم عصرية
- **JavaScript (ES6+)** - منطق التطبيق
- **Chart.js** - الرسوم البيانية
- **LocalStorage** - تخزين محلي

### Backend
- **Node.js** - بيئة تشغيل JavaScript
- **Express.js** - إطار عمل الويب
- **Multer** - معالجة رفع الملفات

### Database & Auth
- **Supabase (PostgreSQL)** - قاعدة بيانات سحابية
- **bcryptjs** - تشفير كلمات المرور
- **JWT** - المصادقة

### AI & Services
- **Google Cloud Vision API** - تحليل صور الطعام

---

## 📋 المتطلبات

1. **Node.js** (الإصدار 18 أو أحدث)
2. **npm** أو **yarn**
3. حساب **Supabase** (مجاني)
4. مفتاح **Google Cloud Vision API** (اختياري)

---

## 🚀 خطوات التشغيل محلياً

### 1. إعداد Supabase

1. اذهب إلى [supabase.com](https://supabase.com) وأنشئ مشروعاً جديداً
2. انسخ رابط URL و Anonymous Key
3. افتح SQL Editor ونفذ الكود في `server/database.sql`

### 2. إعداد Backend

```bash
cd server
npm install
cp .env.example .env
# عدل .env بمفاتيحك
npm start
```

### 3. تشغيل Frontend

افتح `index.html` مباشرة في المتصفح، أو استخدم:

```bash
# باستخدام Python
python -m http.server 5500

# أو باستخدام Node
npx serve .
```

---

## 📡 واجهة برمجة التطبيقات (API)

### المصادقة

| Method | Endpoint | الوصف |
|--------|----------|-------|
| POST | `/api/auth/register` | تسجيل مستخدم جديد |
| POST | `/api/auth/login` | تسجيل دخول |
| GET | `/api/auth/me` | الحصول على بيانات المستخدم |

### الملفات الشخصية

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/profile` | جلب الملف الشخصي |
| POST | `/api/profile` | حفظ الملف الشخصي |

### الوجبات

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/meals` | جلب جميع الوجبات |
| POST | `/api/meals` | إضافة وجبة |
| DELETE | `/api/meals/:id` | حذف وجبة |

### الماء

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/water` | جلب بيانات الماء |
| POST | `/api/water` | تحديث بيانات الماء |

### الصور

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/photos` | جلب صور التقدم |
| POST | `/api/photos` | رفع صورة جديدة |
| DELETE | `/api/photos/:id` | حذف صورة |

### الذكاء الاصطناعي

| Method | Endpoint | الوصف |
|--------|----------|-------|
| POST | `/api/analyze-food` | تحليل صورة الطعام |

---

## 🔒 الأمان

- ✅ تشفير كلمات المرور بـ bcrypt
- ✅ Rate Limiting لمنع الهجمات
- ✅ Helmet للحماية
- ✅ CORS محكم
- ✅ التحقق من المدخلات
- ✅ Row Level Security في Supabase

---

## ☁️ النشر

### Backend (Render/Railway)

1. ارفع المجلد `server` إلى GitHub
2._connect-_to Render أو Railway
3. أضف المتغيرات البيئية:
   - `SUPABASE_URL`
   - `SUPABASE_ANON_KEY`
   - `GOOGLE_VISION_API_KEY`
4. Publish

### Frontend (Netlify)

1. ارفع الملفات `index.html`, `styles.css`, `app.js`
2. أو اجعلها تخدم من نفس الـ Backend

---

## 📱 المميزات

- [x] تتبع السعرات الحرارية
- [x] تحليل صور الطعام بالذكاء الاصطناعي
- [x] تتبع استهلاك الماء
- [x] برامج تمارين مخصصة
- [x] صور التقدم
- [x] المؤشرات الحيوية
- [x] تتبع الإجهاد
- [x] GPS للتدريب
- [x] دعم العربية
- [x] وضع الضيف (بدون تسجيل)
- [x] تسجيل الدخول / إنشاء حساب

---

## 📄 الترخيص

MIT License

---

## 👨‍💻 المطور

تم التطوير بواسطة [ضع اسمك هنا]
